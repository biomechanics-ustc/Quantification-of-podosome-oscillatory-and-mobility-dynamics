Usage Note:
 > Please ensure that the MATLAB "Current Folder" is set to the unzipped code directory before running the scripts. 
The scripts use relative paths to automatically load experimental data from the Exp_data subfolder.